// ── Download notifications ───────────────────────────────────────────────────
// Disabled: real notifications require a backend that tracks actual downloads.
// No fake/simulated notifications.

function initNotifications() {
  // Reserved for future: real-time download notifications via WebSocket/API
}

document.addEventListener('DOMContentLoaded', initNotifications);
