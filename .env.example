# Optional: if you load env vars another way for `node server.js`.
# Copy to `.env` and use a tool that injects them, or set in the shell instead.
# `.env` and `.env.*` are gitignored.
#
# STEAM_API_KEY=
# LUA_UPSTREAM_TEMPLATE=
# CLOUDFLARE_API_TOKEN=
